<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.example.miwokapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MiwokApp">
        <activity android:name=".NumbersclickListener"></activity>
        <activity
            android:name=".PhrasesActivity"
            android:label="Phrases" />
        <activity
            android:name=".coloursActivity"
            android:label="Colours" />
        <activity
            android:name=".FamilyMemebers"
            android:label="Family Members" />
        <activity
            android:name=".NumbersActivity"
            android:label="Numbers" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>